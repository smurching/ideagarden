$('.feedback_error').hide();


<% if @failed_to_post %>

$('.new_feedback').show().before('<p class="feedback_error"><%= j "Message must be between 10 and 1000 characters" %></p>');

<%else%>

$('#existing_feedback').html("<%= escape_javascript render @idea_posting.feedbacks %>");
$('#existing_private_feedback').html("<%= escape_javascript render 'private_feedbacks' %>");
$('#feedback_body').val("");

<%end%>

