
<%= form_for(@idea_posting, :remote => true) do |f| %>
  <% if @idea_posting.errors.any? %>
    <div id="error_explanation">

      <h2><%= pluralize(@idea_posting.errors.count, "error") %> prohibited this idea posting from being saved:</h2>

      <ul>
      <% @idea_posting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


	<%if session[:facebook]%>
		<%= check_box_tag("fb_tag", "true")%>
		<%= label_tag("fb_tag", "Share on Facebook")%>
	<%end%>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %><br />
  </div>

  <div class="field">
    <%= f.label :pitch %><br />
    <%= f.text_field :pitch %><br />
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %><br />
  </div>

  <div class="field">
    <%= f.label :state %><br/>
    <%= f.check_box :state %>
    "Under Execution?"<br/>
  </div>	


<% @tags.each do |tag| %>

<%= check_box_tag(tag,tag)%>
<%= label_tag(tag,tag)%>

<%end%>

<br/>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>



