


<%if @email_url != nil%>
<p>
Please confirm your email to complete your registration: <br/>
<%= link_to "Inbox", @email_url%>
</p>
<%end%>


<h1 id="index_heading">Idea Postings</h1>

<%= form_for(IdeaPosting.new, :id => "new_idea_posting_form", :remote => true) do |f|%>


  <div class="field" id = "name_div">
  	<a id = "form_caret" href="#">▼</a>  	
    <%= f.text_field :name %><br />
  </div>

  <div class="field">
    <%= f.text_field :pitch %><br />
  </div>

  <div class="field">
    <%= f.text_area :description %><br />
  </div>
	
	
	<div class = "form_tags_container">
  		<h5 class = "form_tags_descriptor" >Project Options</h5> <br/>
  		<ul class= "nav nav-pills form_tags"  id = "form_options">
  			<li class = "inactive" id = "under_execution"><a href = "#">Started Project Already?</a></li>
  			<%if session[:facebook]%>
  				<li class = "inactive" id = "facebook"><a href = "#">Post to Facebook</a></li>
  			<%end%>  		
  		</ul>
  	</div>
  	
  	<div class = "form_tags_container">
  		<h5 class = "form_tags_descriptor">Categories</h5> <br/>  	
		<ul class="nav nav-pills form_tags" id = "form_categories">
  			<li class = "inactive" id = "technology"> <a href="#">Technology</a> </li>
  			<li class = "inactive" id = "science"><a href="#">Science & Math</a></li>
  			<li class = "inactive" id = "research"><a href="#">Research</a></li>
  			<li class = "inactive" id = "language"><a href="#">Language</a></li>  
  		</ul>
		<ul class="nav nav-pills form_tags" id = "form_focus">
  			<li class = "inactive" id = "making_things"> <a href="#">Making Things</a> </li>
  			<li class = "inactive" id = "art"><a href="#">Art</a></li>
  			<li class = "inactive" id = "community_service"><a href="#">Community Service</a></li><br/><br/>
  		<div class="actions" id = "form_post_container">
    		<%= f.submit "Share your Idea", :class => "btn btn-success" %>
  		</div> 			
		</ul>
	
	</div>
	


<br/>




<%end%>

<br/>
<div id="index_posting_list">
<%= div_for(@idea_postings) do |idea_posting| %>
  <div class = 'voting'>
  	<div class = 'voting_button'>
    <% if logged_in? %>
      <% if current_user.posting_votes.include? idea_posting.id %>
        <%= button_to '+', unvote_path(idea_posting.id), :class => "vote_button unvote_button", :remote => true %>
      <% else %>
        <%= button_to '+', upvote_path(idea_posting.id), :class => "vote_button upvote_button", :remote => true %>
      <% end %>
    <% else %>
      <%= button_to '+', upvote_path(idea_posting.id), :class => "vote_button upvote_button", :remote => true %>
    <% end %>
    </div>
    <div class = 'potential'>
      <%= idea_posting.potential %>
    </div>
  </div>
  <div class = 'posting'>
    <div class = 'thumbnail'></div>
    <div class = 'brief'>
      <h2 class = 'title'><%= link_to idea_posting.name, idea_posting %></h2>
      <p class = 'owners'>by <% idea_posting.users.each do |user| %><%= link_to user.profile.name + ", ", user_profiles_path(user.id) %><% end %></p>
      <h3 class = 'pitch'>Pitch: <%= simple_format(idea_posting.pitch) %></h3>
      <h3 class = "tags">Tags: 
		<% if idea_posting.tags.length >= 2%>
      	<% (0..idea_posting.tags.length-2).each do |int| %>
    	<%= idea_posting.tags[int].value+", " %>
    	<% end %>
    	<%= idea_posting.tags.last.value%>  
    	<%else%>
    	<% idea_posting.tags.each do |tag| %>
    	<%= tag.value%>
    	<%end%>
    	<%end%>
      </h3>
    </div>
  </div>
<% end %>
</div>

  
